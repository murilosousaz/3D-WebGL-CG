const box = (x, y, z, w, h, d) => ({
  type: 'box',
  pos: [x, y, z],
  size: [w, h, d]
});

// ---- External walls --------------------------------------------------------
const externalWalls = [
  box(  0, 4, -30, 40, 8, 0.3),
  box(-15, 4,  30, 10, 8, 0.3),
  box( 15, 4,  30, 10, 8, 0.3),
  box(-20, 4,   0, 0.3, 8, 60),
  box( 20, 4,   0, 0.3, 8, 60)
];

// ---- Internal walls --------------------------------------------------------
const internalWalls = [
  box(-10, 4, -10, 0.2, 8, 15),
  box( 10, 4, -10, 0.2, 8, 15),
  box(  0, 4,  10, 30,  8, 0.2)
];

// ---- Pedestals -------------------------------------------------------------
const pedestals = [
  box(  0, 0.5, -15, 1.5, 1, 1.5),
  box(  7, 0.5,   0, 1.5, 1, 1.5),
  box( -7, 0.5,   0, 1.5, 1, 1.5),
  box(  0, 0.5,   5, 2.5, 1, 2.5)
];

// ---- Moon pedestals --------------------------------------------------------
const moonPedestals = [
  box(  0, 0.5, -20, 2.0, 1, 2.0),
  box(-12, 0.5, -25, 1.5, 1, 1.5),
  box( 12, 0.5, -25, 1.5, 1, 1.5)
];

// ---- Export ---------------------------------------------------------------
export const collisionMap = Object.freeze([
  ...externalWalls,
  ...internalWalls,
  ...pedestals,
  ...moonPedestals
]);
